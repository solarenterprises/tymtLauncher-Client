import { ChatMessageType } from "../../types/chatTypes";

export interface IParamsBubbleFile {
  message: ChatMessageType;
  decryptedMessage: string;
}

const BubbleFile = ({}: IParamsBubbleFile) => {
  return <></>;
};

export default BubbleFile;
