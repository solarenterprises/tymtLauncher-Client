import { ChatMessageType } from "../../types/chatTypes";

export interface IParamsBubbleVideo {
  message: ChatMessageType;
  decryptedMessage: string;
}

const BubbleVideo = ({}: IParamsBubbleVideo) => {
  return <></>;
};

export default BubbleVideo;
