import { ChatMessageType } from "../../types/chatTypes";

export interface IParamsBubbleImage {
  message: ChatMessageType;
  decryptedMessage: string;
}

const BubbleImage = ({}: IParamsBubbleImage) => {
  return <></>;
};

export default BubbleImage;
